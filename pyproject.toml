[project]
name = "torchlings"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.2.1",
    "numpy>=2.3.1",
    "pytest-xdist>=3.8.0",
    "torch>=2.7.1",
]

[tool.pytest.ini_options]
python_files = ["*.py"]